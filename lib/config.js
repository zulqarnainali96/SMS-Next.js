// Replace this with your backend URL — used everywhere the app calls the School Management API.
export const BASE_URL = "http://127.0.0.1:8000";

// Optional: set your school UUID here for create operations when the logged-in profile has no school.
export const DEFAULT_SCHOOL_ID = "";
